name: Azure Login
description: Authenticate with Azure using service principal credentials

inputs:
  azure_credentials:
    description: 'Azure credentials JSON'
    required: true

runs:
  using: composite
  steps:
    - name: Azure Login
      uses: azure/login@v2
      with:
        creds: ${{ inputs.azure_credentials }}
